# CLAUDE.md — sql-design-thinking

このファイルは Claude Code がセッション開始時に自動で読み込む、本リポジトリの前提・方針・禁止事項をまとめたものです。

---

## 1. プロジェクト概要 / Overview

ビジネスデータ分析およびデータ基盤構築で実際に使用している **SQL設計パターンとデータモデリングの考え方** をまとめた公開リポジトリ。

- 単なる「SQLの書き方」集ではなく、**なぜその設計にしたか**を記録することが主目的
- 掲載SQLは実務ロジックをベースに、企業情報・機密情報を除去した **抽象化サンプル**
- 本リポジトリは **Public**。公開して問題ない情報のみを扱う

---

## 2. 実行環境の前提 / Environment

- **DB: Snowflake**。SQLは必ず Snowflake 構文で書く（`QUALIFY`、`LISTAGG ... WITHIN GROUP`、`GREATEST`、`REGEXP_LIKE` 等の利用を前提とする）
- 実務環境固有の事情は捨象し、**素の Snowflake SQL として単体で読める形**にする
- dbtライクな3層アーキテクチャ（Staging → Intermediate → Marts）を設計の基本骨格とする

---

## 3. ディレクトリ構成 / Structure

```text
sql-design-thinking/
├── CLAUDE.md                  ← このファイル
├── README.md                  ← 全体アーキテクチャ（mermaid図つき）
├── advanced_sql_recipes/      ← 汎用的なSQL設計パターン（単体で完結）
└── business_cases/            ← 実務ベースのユースケース（複数クエリ構成）
     └── <case_name>/
          ├── README.md
          ├── 01_staging/
          ├── 02_intermediate/
          └── 03_marts/
```

各ケースの README には、上流・下流ケースへの相互参照（Upstream / Downstream）を必ず記載し、リポジトリ全体が **ひとつのデータ基盤として繋がって読める**状態を維持する。

---

## 4. 【最重要】機密情報の取り扱い / Security

**このセクションの違反は、他のあらゆる方針に優先して回避すること。**

### 絶対に守るルール

- 実務の生クエリ・生データは、**このリポジトリに一切コミットしない**
- 生クエリは Claude Code に直接置かず、別経路で内容を渡し、**一般化された成果物のみ**をリポジトリに追加する
- ローカルに作業用ディレクトリ（`_raw/`、`_local/`、`_wip/`）を作る場合は、**必ず先に `.gitignore` に登録してから**ファイルを置く
- 迷ったら「載せない」を選ぶ。Publicリポジトリは push した時点で履歴に残り、後から完全に消すのは困難

### `.gitignore` に必ず含める

```gitignore
_raw/
_local/
_wip/
*.csv
*.xlsx
.env
```

### 公開前チェックリスト（コミット前に毎回確認）

- [ ] 実テーブル名・スキーマ名・DB名が残っていないか
- [ ] 社内システムの実名が残っていないか
- [ ] 実際の金額・件数・顧客ID・メールアドレス・商品名が残っていないか
- [ ] 社内特有の略語や、部署名・担当者名が残っていないか
- [ ] コメント欄・カラムコメント・README に上記が紛れ込んでいないか（**本文よりコメントに残りやすい**）

---

## 5. 一般化ルール / Generalization Rules

実務クエリを公開用に加工する際の変換基準。

### 置換の指針

| 対象 | 置換後の例 |
|---|---|
| 実テーブル名 | `raw_orders`, `stg_all_purchases_base`, `fct_all_purchases_unified` など役割が分かる汎用名 |
| マスタテーブル | `dim_products`, `dim_customers`, `dim_subscription_info` |
| システム名 | `SYSTEM_A` / `SYSTEM_B` |
| 商品ID・カテゴリ値 | `A`, `B`, `RG` のような記号、またはダミー値 |
| 金額・件数 | 桁感だけ残したダミー値 |

### 加工時の原則

- **ロジックの本質と設計意図は削らない**。除去するのは固有名詞と実データであり、アルゴリズムの複雑さそのものは温存する
- 抽象化した結果、そのSQLが**単体で読んで意味が通るか**を必ず確認する
- 除去によって前提が失われる場合は、README に「◯◯という業務前提がある」と**言葉で補足**する
- 「なぜこの設計にしたか」の解説を必ず添える。**設計理由のない実装は未完成**とみなす
- ブラックボックス化を避ける。監査可能性（Auditability）を優先し、挙動が説明できない手法は採用しない

---

## 6. 記述スタイル / Style Conventions

### 言語

- 説明・コメントは **日本語を主、英語を従** とする（README・ヘッダーコメントは日英併記）
- 出力カラムのエイリアスは、BI利用を想定して日本語名を使う場合がある（既存クエリの流儀に合わせる）

### SQLファイルのヘッダーコメント

全SQLファイルの冒頭に、以下の構造のコメントブロックを置く（既存ファイルの形式を踏襲すること）。

```text
【クエリ概要 / Query Overview】
【業務ロジック / Business Logic】     … 番号付きで処理の狙いを説明
【CTE構造 / CTE Structure】           … 各CTEの役割を順に記載
【データ粒度 / Data Grain】
【主キー / Primary Key】
【実行環境 / Execution Environment】
【出力データ / Output Dataset】
```

### 命名規則

- ファイル: `NN_<layer>_<name>.sql`（例: `01_stg_order_amount.sql`、`04_int_cancellation_return.sql`、`05_fct_all_purchases_unified.sql`）
- レイヤー接頭辞: `stg_`（Staging） / `int_`（Intermediate） / `fct_`・`dim_`（Marts）
- CTE名は処理内容が読める動詞・名詞句にする（例: `find_dummy_pairs_gate_1`、`resolve_rank_2_pairs`、`evaluate_conflict_rank`）
- 各CTEの直前に、区切り線コメントと `Data Grain:` の明記を入れる

### README の構成

概要（日英） → リポジトリ全体における位置づけ（Upstream / Downstream） → パイプライン・アーキテクチャ（mermaid図） → 主要な設計パターンと解決した課題 → データ構造 → データ品質戦略 → 運用と保守。

---

## 7. 設計思想 / Design Philosophy

- **透明性 > 自動化**。ブラックボックスなツールより、監査できるSQLを選ぶ
- **負の知識を残す**。うまくいった方法だけでなく、「なぜその方法を採らなかったか」を明記する
- **推測処理には必ず品質指標を付ける**。ヒューリスティックな紐付けを行うクエリには、マッチング強度の分布などを SLI（Service Level Indicator）としてカラム出力し、劣化を監視可能にする
- **安全側に隔離する**。誤結合ノイズを生みうる緩い判定は、通常プールに混ぜず専用の隔離プールで処理する（EQUIVALENTゲートの設計思想）
- **最終判断は人間が行う**。AIの生成物はレビュー対象であり、そのまま採用しない

---

## 8. 負の知識 / Known Pitfalls

過去に実際に踏んだ落とし穴。**同じ轍を踏まないこと。新たに踏んだら随時ここに追記する。**

### SQL構文レベル

- **CTE末尾のカンマ**: CTEを削除・並べ替えした際に取り残されやすい。編集後は必ず確認する
- **JOIN の ON 句で SELECT のエイリアスは参照できない**。サブクエリ・CTEで先に確定させる
- **`REGEXP_LIKE` は完全一致判定**。部分一致のつもりで書くと外れる。部分一致は `.*` を明示するか `REGEXP_SUBSTR` / `LIKE` を検討する
- **`LIKE` のエスケープ**: `_` や `%` を含む文字列を検索する際は `ESCAPE` 句を忘れない

### 設計レベル

- **コホート軸の取り違え（注文日 vs 出荷日）**: 相互依存する複数クエリにまたがると、片方だけ直しても整合しない。**軸を変更する場合はパイプライン全体で一括して揃える**
- **JOIN爆発**: 1親に対して複数の子（返品の枝番等）が存在する場合、単純JOINで金額が多重計上される。結合前に重複排除するか、`LISTAGG` で1行に集約してから結合する
- **N対Mの取り合い**: 推測マッチングでは、同じ相手を複数候補が奪い合う。ランク付け＋Anti-Join のカスケードで1対1を担保する
- **端数処理のズレ**: 全体割引を明細に単純按分すると、決済額と1〜2円ずれる。累積和と `LAG` で端数を吸収させる
- **緩い判定を通常プールに混ぜない**: 救済目的の寛容なマッチングは、隔離プールを新設して判定する

---

## 9. 作業の進め方 / Workflow

- 変更は必ず **ブランチを切って** 行い、`main` に直接コミットしない
- push 後は **PRの差分を人間が目視レビュー** してからマージする（機密情報の最終確認を兼ねる）
- マージ後、役目を終えたブランチは削除する
- コミットメッセージには「何を変えたか」に加えて「なぜ変えたか」が分かる情報を含める
- 大きな変更を行った場合、該当ケースの README と本ファイルの更新漏れがないか確認する
